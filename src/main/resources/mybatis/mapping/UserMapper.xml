<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jjcc.bootlaunch.generator.test1.UserMapper">

    <resultMap id="BaseResultMap" type="com.jjcc.bootlaunch.model.User">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
    </resultMap>

    <sql id="user_column">
        id, username, password
    </sql>

    <delete id="deleteUserInfo">
        delete from user where id = #{id}
    </delete>

    <select id="getUserAll" resultMap="BaseResultMap">
        select <include refid="user_column"/> from user
    </select>

    <select id="getUserInfo" resultMap="BaseResultMap">
        select <include refid="user_column"/> from user where id = #{id}
    </select>


    <select id="selectUserDispose" resultType="com.jjcc.bootlaunch.model.User">
        select <include refid="user_column"/> from user
        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="user.id != null">
                and id = #{user.id}
            </if>
            <if test="user.username != null and user.username != '' ">
                and username = #{user.username}
            </if>
            <if test="user.password != null and user.password != '' ">
                and password = #{user.password}
            </if>
        </trim>
    </select>


</mapper>